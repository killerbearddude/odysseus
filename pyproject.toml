[tool.pytest.ini_options]
markers = [
    "unit: fast unit tests",
    "security: security boundary tests",
    "integration: tests requiring external or composed services",
    "docker: tests requiring docker compose",
    "slow: slow tests",
]
testpaths = ["tests"]
asyncio_mode = "auto"
